# Akura AI - Environment Configuration
# Copy this file to .env and update with your values

# Server Configuration
HOST=0.0.0.0
PORT=8000
DEBUG=false
ENVIRONMENT=production

# Ollama Configuration
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=hf.co/hasinduOnline/akura_ai_sinhala_dyslexic_word_corrector_4bit:Q4_K_M
OLLAMA_TIMEOUT=120

# Model Configuration
MODEL_TEMPERATURE=0.3
MODEL_MAX_TOKENS=512
AI_CONFIDENCE_THRESHOLD=0.7

# Logging
LOG_LEVEL=INFO
LOG_FORMAT=json

# CORS Configuration (comma-separated origins)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,http://localhost:8080

# Rate Limiting
RATE_LIMIT_REQUESTS=100
RATE_LIMIT_PERIOD=60

# LLM Provider: "ollama" or "gemini"
# Use "ollama" for local fine-tuned model (no internet required for text analysis)
# Use "gemini" for cloud-based Gemini (requires GEMINI_API_KEY)
LLM_PROVIDER=ollama

# Gemini Configuration (used for OCR; also used for LLM when LLM_PROVIDER=gemini)
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-2.0-flash

# Database Configuration (Supabase PostgreSQL)
DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@db.YOUR_PROJECT.supabase.co:5432/postgres

# ============================================
# External Microservices Configuration
# ============================================
# API Gateway URL (routes to all microservices)
API_GATEWAY_URL=http://localhost:8000

# Enable external services (set to true to use gateway-routed services)
USE_EXTERNAL_OCR=false
USE_EXTERNAL_PATTERNS=false

# Direct service URLs (optional - use if not going through gateway)
# OCR_SERVICE_URL=http://localhost:8001
# PATTERN_SERVICE_URL=http://localhost:8002

